import styled from "styled-components";

const PizzaSectionWrapper = styled.main`
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: space-between;
`;

export default PizzaSectionWrapper;
